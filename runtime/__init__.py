"""Ligandparam-owned runtime helpers (console, job boards, CPU split).

Copied from the historical ffpopt runtime so parameterization does not
import ffpopt. ffpopt keeps its own copies under ``src/ffpopt/runtime``.
"""
